<div class="container" style="margin-left:100px;">
  <div>
    <%= link_to "投稿", new_post_path %>
  </div>
  <div class="row d-flex justify-content-between" style="width:100%;">
  <% @posts.each do |post| %>
    <div style="width:35%; height:425px; background-color:blue;">
      <%# image_tag post.user.avater_image.variant(resize: "65x65^", gravity: "center", crop: "65x65+0+0").processed, style: "border-radius: 50%;" %>
    </div>
    <div style="width:60%; height:425px; background-color:red;">
      <%= post.posted_text %>
    </div>
  <% end %>
  </div>
</div>

<%# 投稿したアカウントを表示するようにする
postから情報を持ってくればうまくいくはず %>
